<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.AppBarLayout
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:theme="@style/AppTheme.AppBarOverlay"
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto">

    <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_height="?attr/actionBarSize"
            android:layout_width="match_parent"
            android:background="?attr/colorPrimary"
            app:popupTheme="@style/AppTheme.PopupOverlay">

        <GridLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="horizontal"
                android:rowCount="1"
                android:columnCount="3">

            <ImageView
                    android:id="@+id/bar_list"
                    android:src="@drawable/ic_list"
                    android:onClick="onToolbarItemClick"
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:layout_marginTop="4dp"
                    android:layout_marginBottom="4dp"
                    android:layout_gravity="center"
                    android:layout_columnWeight="20"/> <!--android:layout_marginRight="?attr/actionBarSize"-->

            <ImageView
                    android:id="@+id/bar_addreminder"
                    android:src="@drawable/ic_addreminder"
                    android:onClick="onToolbarItemClick"
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:layout_marginTop="4dp"
                    android:layout_marginBottom="4dp"
                    android:layout_gravity="center"
                    android:layout_columnWeight="20"/>

            <ImageView
                    android:id="@+id/bar_settings"
                    android:src="@drawable/ic_settings"
                    android:onClick="onToolbarItemClick"
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:layout_marginTop="4dp"
                    android:layout_marginBottom="4dp"
                    android:layout_gravity="center"
                    android:layout_columnWeight="20"/>

        </GridLayout>

    </android.support.v7.widget.Toolbar> <!--app:layout_scrollFlags="scroll|enterAlways"-->

</android.support.design.widget.AppBarLayout>